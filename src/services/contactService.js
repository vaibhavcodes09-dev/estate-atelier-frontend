// contactService.js — placeholder service for contact form submissions.
// All functions are empty stubs with TODO comments.
// Will be wired to Supabase data layer in a future implementation.

export async function submitContactForm(data) {
  // TODO: insert a contact submission into Supabase
  console.log('submitContactForm', data);
}

export async function getContactSubmissions() {
  // TODO: fetch contact submissions (admin use)
  console.log('getContactSubmissions');
  return [];
}
